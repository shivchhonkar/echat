services:
  mongodb:
    image: mongo:7
    container_name: echat_mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: echat_server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "6000:6000"
    depends_on:
      - mongodb

  client:
    build:
      context: .
      dockerfile: client/Dockerfile
    container_name: echat_client
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5173:5173"
    depends_on:
      - server

volumes:
  mongodb_data:
